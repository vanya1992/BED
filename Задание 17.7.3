per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}
money = (input("введите сумму"))
money = float(money)
per_cent['ТКБ'] = money * per_cent['ТКБ'] / 100
per_cent['СКБ'] = money * per_cent['СКБ'] / 100
per_cent['ВТБ'] = money * per_cent['ВТБ'] / 100
per_cent['СБЕР'] = money * per_cent['СБЕР'] / 100
deposit = list (per_cent.values ())
deposit = max(deposit)
print("Максимальная сумма, которую вы можете заработать —", deposit)
